package craft.level.tile;

public class SoilTile extends Tile{
	protected SoilTile(int id, int tex, int destroyTime, boolean soildTile, boolean lightTile) {
		super(id, tex, destroyTime, soildTile, lightTile);
	}
}
